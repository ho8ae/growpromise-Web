version: '3.8'

services:
  frontend:
    build:
      context: ./front
      dockerfile: Dockerfile
    container_name: frontend-growpromise
    restart: always
    environment:
      - NODE_ENV=production
    networks:
      - growpromise-network

networks:
  growpromise-network:
    external: true